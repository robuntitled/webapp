# Old Composer UI (backup)

Snapshot of the previous trip-composer plan UI and map before the split-screen UX refactor.

These files are **not imported** by the app. Restore them only if you need to recover the prior layout.

| Backup | Original |
|--------|----------|
| `ComposerPlanStep.backup.tsx` | `components/composer/ComposerPlanStep.tsx` |
| `plan/*.backup.tsx` | `components/composer/plan/*` |
| `maps/TripMap.backup.tsx` | `components/maps/TripMap.tsx` |
